name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
          
      - name: Run Unit Tests
        run: |
          npm install
          npm run build
          npm run test
          
